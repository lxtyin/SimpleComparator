#include <bits/stdc++.h>
using namespace std;

void solve() {
    double n; cin >> n;
    cout << setprecision(8) << fixed << n << '\n';
}

signed main() {
#ifdef ACM_LOCAL
    freopen("x.in", "r", stdin);
#endif
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int T = 1;
    // cin >> T;
    while(T--) {
        solve();
    }
    return 0;
}
